# 2026-08-21

## share-rl 代码库框架梳理（框架大纲.md）
- 完成 share-rl 全库源码精读并输出《框架大纲.md》（项目根）。
- 项目定位：LeRobot 扩展，MP-Net（操作原语图）驱动结构化操作 + HIL 在线 SAC/DAgger 训练。
- 核心抽象链：TaskFrame（轴级契约，policy_mode!=None 即可学轴，绝对旋转轴流形编码 S1/S2/SO(3)6D）→ ManipulationPrimitiveConfig（draccus，on_entry 入口钩子，子类 primitive/zero_ft/move_delta/open_loop_trajectory/foundation_pose）→ MP-Net（图编排，start/reset/transitions，BFS 可达性校验）。
- 单步数据流：action_processor（事件注入→ToNestedAction→遥操作对齐→干预选择→夹爪离散化）→ env.step → env_processor（FK注入→StateObservation→图像处理）→ transition 检查。
- 转移类型 11 种（always/on_event/on_success/on_failure/on_obs_threshold/on_time_limit/on_target_pose_reached/reward_classifier/on_info_equals/all_of/record_world_pose）。
- RL：share.rl.runtime 的 build_adaptive_registry（每自适应原语一份 SAC + online_steps 预算），actor/learner 经 gRPC（LearnerService）同步参数与转移；SACDaggerBCPolicy 在干预段离线数据上做 BC；offline buffer 来源=on-policy 干预转移重建 + 外部 demos。
- 硬件：UR v1（RTDE 子进程+共享内存 IPC）/ UR v2（控制器可插拔：force_mode vs direct_torque τ=JᵀF）；遥操作 delta_keyboard/spacemouse（delta vel）、widowx/gello（绝对关节）。
- **已知坑**：share-train→scripts/train.py 缺失；share-workspace→robot_workspace.py 缺失；workspace/runtime.py 引用不存在的 providers/store/tools；foundationpose 在 share/primitives 与 experiments 重复注册同名子类；gello name="widowx" 且 calibrate/send_feedback 为 stub；design_constraints 的 save/reset_bounds 被注释禁用。
- 已核实缺失模块归属：providers/store/tools/train.py/robot_workspace.py 在 git 全分支历史中**从未提交过**（git log --all 为空），且均属 share.* 命名空间，LeRobot 主库无 workspace 目录（在线核对 src/lerobot 目录列表）→ 全部是 share-rl 自研未落地代码，非 LeRobot 提供；唯一例外 train.py 的设计意图是薄包装 LeRobot 训练入口（lerobot/scripts 存在）。
- 工作区有未提交的 MuJoCo 插入实验改动：mujoco_camera.py、configs/mujoco_insertion.py、robots/mujoco/、scripts/mujoco_demo.py、wiki/mujoco.rst、tests/share/robots/（新增）+ record.py/actor_server.py/learner_server.py/env_manipulation_primitive_net.py/pyproject.toml 等（修改）。
- 确认原论文 = SHaRe-RL: Structured, Interactive RL for Contact-Rich Industrial Assembly Tasks（arXiv:2509.13949, ICRA 2026），一作 Jannick Stranghöner（= share-rl 仓库作者 jstranghoener）。
- 论文三支柱↔代码映射：①MP-Nets/AMPs（可学习轴=n 维动作空间）↔ TaskFrame.learnable_axis_indices/ManipulationPrimitive(Net)；②HIL-RL（SpaceMouse 演示缓冲+在线干预替换、离线/在线缓冲等比例采样、RLPD loss）↔ InterventionActionProcessorStep/SpaceMouse teleop/SACDaggerBCPolicy/learner 双缓冲；③自适应力限制 Eq(1)-(4)：α(F)=α_min+(1-α_min)exp(-F/θ)，稳定性 |α'(F*)|F_max<1 ↔ utils/transformation_utils.exp_scale(f_meas,f_thresh,s_min=0.2,theta=0.1)（与论文 Eq(4) 逐项对应）、URConfig.compliance_adaptive_limit_theta、WrenchMonitorHistory。
- 论文实验数值：自由空间平移 7N/旋转 2Nm，接触态缩放后 2N/0.4Nm；任务 x,y,c 三轴笛卡尔速度设定值。
- 输出《目录总览.md》：src/share 全部 14 个子包 + experiments/examples/tests/tools/wiki/calibration 逐文件功能清单（约 130 文件）；含 ⚠️ 未提交（mujoco_*、mujoco_insertion、mujoco_demo、mujoco_camera、robots/mujoco/、wiki/mujoco.rst、tests/share/robots/）与不可用（train.py、robot_workspace.py、workspace runtime 依赖缺失）标注。
- 输出《论文-代码对照索引.md》（三支柱带行号）：①结构↔task_frame.py:94-147（policy_action_dim/learnable_axis_indices）+ config/env MP + transitions; ②交互↔action.py:393-443 干预覆盖、env_mpnet.py:216-219 记录干预动作、learner_server.py:557-592 干预转移入 offline buffer、L241-281 等比例采样、L347-379 BC-DAgger、modeling_sac_dagger.py:34-53; ③安全↔transformation_utils.py:220-227 exp_scale（Eq4）、config_ur.py:72-80 参数/L168 compute_theta/L181-189 稳定性、controller.py:1442-1540 apply_wrench_bounds、v2 base.py:893、direct_torque.py:27-69 τ=JᵀF。系统级：actor_server/learner_server gRPC。
- **用户反馈（重要）**：带行号的索引文档太抽象——用户懂论文/HIL-SERL/SAC，但看不懂索引式文档。改为"文件夹→人话比喻"（工序图纸/翻译官/策略大脑/机器人身体/人手柄/训练工具箱/按钮/杂货铺）+ 两张内联图（一次 step 数据循环、仓库功能地图三区：运行时蓝/训练琥珀/支撑绿）。后续讲解 share-rl 一律走"比喻 + 图 + HIL-SERL 对照"，少堆行号。
