# SHaRe-RL 论文三支柱 → 代码对照索引（带行号）

> 论文：**SHaRe-RL: Structured, Interactive Reinforcement Learning for Contact-Rich Industrial Assembly Tasks**（arXiv:2509.13949，ICRA 2026）
> 代码：share-rl（本仓库，论文官方实现，作者同名）
> 行号基于当前工作区（含未提交改动）。路径省略前缀 `src/share/`。

---

## 支柱总览

| 支柱 | 论文出处 | 代码目录 |
|---|---|---|
| ① 结构 Structure（MP-Nets / AMPs） | Method III-B | `envs/manipulation_primitive*`、`processor/action.py` |
| ② 交互 Interaction（HIL-RL） | Method III-A | `processor/`、`policies/sac_dagger/`、`scripts/{record,actor_server,learner_server}.py` |
| ③ 安全 Safety（自适应力限制） | Method III-C | `utils/transformation_utils.py`、`robots/ur/`（controller、config_ur）、`robots/mujoco/` |

---

## 支柱一：结构（MP-Nets / 自适应原语）

### 论文关键论断

> "Classical MPs are executed with fixed control setpoints. Extending this notion, adaptive MPs (AMPs) **replace selected setpoints by policy-controlled variables**."
> "If **n axes are controlled by the policy**, the corresponding action space is **n-dimensional**."
> "The operator's SpaceMouse is likewise constrained to these n axes, ensuring that both demonstrations and interventions remain aligned with the degrees of freedom available to the policy."

### 代码对照

| 论文概念 | 代码位置 | 说明 |
|---|---|---|
| 控制空间/模式枚举（TASK/JOINT、POS/VEL/WRENCH、ABS/REL） | `envs/manipulation_primitive/task_frame.py:9-29` | `ControlSpace`(L9)、`PolicyMode`(L16)、`ControlMode`(L23) |
| **"替换设定值为策略变量"** = 每轴 `policy_mode != None` | `task_frame.py:94-102` | `learnable_axis_indices`(L94)、`is_adaptive`(L99) |
| **"动作空间 = n 维"** | `task_frame.py:104-138` | `policy_action_dim`：非绝对旋转轴每轴 +1；绝对旋转轴流形编码 +2/+3/+6（L117-133） |
| 绝对旋转流形表示（S¹/S²/SO(3)6D） | `task_frame.py:140-147, 174-201` | `is_absolute_rotation_axis`、`policy_action_keys`（`cos/sin`、`rotation.s2.*`、`rotation.so3.*`） |
| 原语级 is_adaptive | `envs/manipulation_primitive/config_manipulation_primitive.py:203-206` | 任一任务帧 `policy_action_dim > 0` |
| 原语入口：写入目标设定值 | `config_manipulation_primitive.py:605-620` | `on_entry` → `env.set_target_pose` |
| 入口/目标位姿解析（AMP 替换的设定值来源） | `config_manipulation_primitive.py:622-641` | `resolve_targets`；子类 `move_delta`(L693-810)、`open_loop_trajectory`(L813-971) |
| 原语运行时状态（目标/完成/进度） | `envs/manipulation_primitive/env_manipulation_primitive.py:16, 49-74, 164-211` | `ManipulationPrimitive.step`(L49)、`reset_runtime_state`(L164)、`set_target_pose`(L187)、`apply_task_frames`(L158) |
| MP-Net 图（start/reset/primitives/transitions） | `envs/manipulation_primitive_net/config_manipulation_primitive_net.py:35-95` | `__post_init__` 不变量校验：非终态必须有出边(L77-83)、终态从 start 可达 BFS(L85-95) |
| 图编排器（切换原语、整集重置路径） | `envs/manipulation_primitive_net/env_manipulation_primitive_net.py:179-277, 324-420` | `_step_env_and_check_transitions`(L179)、`_full_reset`(L324)、`_enter_active_primitive`(L360)、`_default_target_pose_axes`(L279) |
| 声明式图边（何时切换原语） | `envs/manipulation_primitive_net/transitions.py:31-43, 324-383` | `Transition.evaluate` 契约(L38)；`OnTargetPoseReached`(L324) 用于"设定值到位"判定 |
| **"SpaceMouse 也约束到 n 轴"** | `processor/action.py:258-298`（编码）、`175-223`（delta 映射） | `MatchTeleopToPolicyActionProcessorStep._encode_learning_space` 只编码可学轴 → 遥操作被投影到与策略相同的 n 维空间 |
| 结构应用示例（13 原语装配图） | `experiments/envs/foundationpose/ur5e_foundationpose_pick.py` | scan→estimate→grasp→insert 全编排 |

---

## 支柱二：交互（HIL-RL：演示 + 在线干预 + 双缓冲）

### 论文关键论断

> "Before training begins, a skilled operator collects a set of teleoperated trajectories with a six-DoF SpaceMouse, which are stored in a **demonstration buffer**."
> "During training, the operator can press a button to take control of the robot at any time. **The policy action is then replaced by the human SpaceMouse action** for the duration of the intervention."
> "The learner process samples mini-batches **equally from the demonstration buffer and an on-policy buffer** to performs gradient updates on the **RLPD loss**."
> "Updated network weights are **streamed back to the actor** at fixed time intervals."
> "the operator can also deliberately induce rare but relevant failure cases, forcing the policy to learn robust recovery behavior."

### 代码对照

| 论文概念 | 代码位置 | 说明 |
|---|---|---|
| 干预事件（按钮）定义 | `teleoperators/utils.py:5-29` | `TeleopEvents`(L5)：`IS_INTERVENTION`(L9)、`SUCCESS/FAILURE`(L6-7)；`has_event`/`is_intervention`(L18) |
| 干预标志注入 info | `processor/info.py:62-98` | `AddTeleopEventsAsInfoStep`（遥操作事件并进 info） |
| 键盘/脚控绑定事件 | `processor/info.py:169-235, 106-166` | `AddKeyboardEventsAsInfoStep`、`AddFootswitchEventsAsInfoStep` |
| 干预时读取遥操作动作 | `processor/info.py:36-54` | `AddTeleopActionAsComplimentaryDataStep`：仅 `IS_INTERVENTION` 时 `teleop.get_action()`(L48-51) |
| **"策略动作被 SpaceMouse 动作替换"** | `processor/action.py:393-443` | `InterventionActionProcessorStep.__call__`：`is_intervention` 时用 `teleop_action_dict` 覆盖(L410-414)，否则用策略动作并给遥操作反馈(L416-423)；leader 力矩切换(L388-391) |
| 干预动作被记录进转移（数据关键！） | `envs/manipulation_primitive_net/env_manipulation_primitive_net.py:216-219` | 干预时 `action_to_record = complementary_data[TELEOP_ACTION_KEY]` |
| 录制端保存干预段 | `scripts/record.py:67-195` | `record_loop`：`rl.is_intervention` 特征(L160)、`save_only_interventions` 模式(L135-141, 146)、每原语独立数据集(L222) |
| **演示缓冲（训练前采集）** | `scripts/learner_server.py:676, 779-790` | `initialize_offline_replay_buffers`(L676)：外部来源 `dataset_root/{primitive}` 或 `offline-demos/{primitive}`(L789-790) |
| **干预转移重建进离线缓冲** | `scripts/learner_server.py:557-592` | `process_transitions`：在线转移入 online buffer(L588)，`is_intervention=True` 同时入 offline buffer(L590-592)；resume 时从历史数据集重建(L635, 707-715) |
| **在线/离线等比例采样（RLPD 式）** | `scripts/learner_server.py:241-281` | `online_batch_size`/`offline_batch_size`(L255-272)；SAC 更新用 `concatenate_batch_transitions` 混合(L387-390) |
| RLPD/SAC 梯度更新 | `scripts/learner_server.py:347-451` | `optimize_policy_once`：SAC 分支(L383-390)、`prepare_forward_batch`(L453) |
| 失败案例学习 = DAgger BC 更新 | `scripts/learner_server.py:361-379` | BC 分支：`policy.forward(batch, model="bc")`(L368)、`loss_bc.backward()`(L371) |
| BC 损失定义 | `policies/sac_dagger/modeling_sac_dagger.py:34-53` | `compute_loss_bc`：$L_{bc}=-\mathbb{E}[\log\pi_\theta(a|s)]$(L52) + MSE(L51)；配置 `bc_lr`(`configuration_sac_dagger.py:11`) |
| **"权重流式回流 actor"** | `scripts/learner_server.py:215, 337, 530`；`rl/runtime.py:372-404` | `push_all_actor_policies_to_queue`；actor 侧 `apply_parameter_updates_from_queue` |
| actor 执行策略采集经验 | `scripts/actor_server.py:186-450` | `act_with_policy`：干预统计(L372-374)、带 `is_intervention` 的 Transition(L380-393)、段指标上报(L216-246) |

---

## 支柱三：安全（逐轴自适应力限制）

### 论文公式

$$F_{\mathrm{lim},t}(F_{\mathrm{meas},t}) = \alpha(|F_{\mathrm{meas},t}|)\,F_{\max} > F_{\mathrm{cmd},t} \tag{1}$$

$$F_{\mathrm{meas},t+1} = F_{\mathrm{lim},t} = \alpha(F_{\mathrm{meas},t})\,F_{\max} \tag{2}$$

$$\left|\alpha'\left(F_{\mathrm{lim}}^{*}\right)\right| F_{\max} < 1 \tag{3}$$

$$\alpha(F) = \alpha_{\min} + (1 - \alpha_{\min})\exp(-F/\theta) \tag{4}$$

> 实验数值：自由空间 平移 7 N / 旋转 2 N·m；接触态缩放后 2 N / 0.4 N·m；传感器噪声 ±2 N。

### 代码对照

| 论文概念 | 代码位置 | 说明 |
|---|---|---|
| **Eq(4) α(F) 直接实现** | `utils/transformation_utils.py:220-227` | `exp_scale(f_meas, f_thresh, s_min=0.2, theta=0.1)`：$s_{\min}+(1-s_{\min})e^{-F/\theta}$ 逐项对应 |
| 参数容器：$F_{\max}$ | `robots/ur/lerobot_robot_ur/config_ur.py:72` | `wrench_limits`（默认 30/3） |
| 参数容器：$\alpha_{\min}$、$\theta$、目标力 | `config_ur.py:76-80` | `compliance_adaptive_limit_enable`(L76)、`compliance_desired_wrench`(L78)、`compliance_adaptive_limit_theta`(L79)、`compliance_adaptive_limit_min`(L80) |
| **$\theta^*$ 解析（设定唯一不动点）** | `config_ur.py:95-165, 168` | `__post_init__` 计算 θ(L120-165)、`compute_theta`(L168，论文"analytically solving for θ*"） |
| **Eq(3) 稳定性检查** | `config_ur.py:181-189` | `exp_scale_and_derivative`：$s'(f)=-(1-s_{\min})/\theta\, e^{-f/\theta}$(L185)、`g_prime = F_max * ds_df`(L145) 并检测 |g'|<1 |
| 运行时每轴力限制应用 | `robots/ur/lerobot_robot_ur/controller.py:1442-1540` | `apply_wrench_bounds`：enable 检查(L1464)、`scaled_wrench_limits = scale_vec * F_max`(L1479)、指令力 clip(L1484/1507) —— Eq(1) 的 `F_lim > F_cmd` |
| 力指令计算→限制调用点 | `controller.py:916-961` | `_compute_task_wrench`：L954 调 `apply_wrench_bounds` |
| 控制环主流程 | `controller.py:424-513, 672-739` | `_run_control_loop`(L432)、`_update_virtual_targets`(L672)、`_compute_output_command`(L709，force 分支 L726) |
| 柔顺参数跨原语切换 | `controller.py:1166-1199` | `_resolve_compliance_settings`（每原语 controller_overrides 触发，L1196 重算 θ） |
| 力执行路径（forceMode） | `controller.py:964-977` | `_send_task_wrench`→`rtde_c.forceMode`(L966)、`_enter_task_force_mode`→`forceModeSetGainScaling`(L977) |
| 六轴力监控（论文安全曲线图源） | `robots/ur/lerobot_robot_ur/wrench_monitor.py` | `WrenchMonitorHistory`/`WrenchMonitorProcess`：measured/filtered/nominal/commanded/**adaptive_limit** 六轴渲染 |
| v2 控制器同机制 | `robots/ur/lerobot_robot_ur_v2/controllers/base.py:415-452, 549, 893` | `_compute_output_command`(L415)、`_compute_task_wrench`(L549)、`apply_wrench_bounds`(L893)、`_send_output_command`→`_send_task_wrench`(L444-452) |
| 直接力矩路径（接触任务变体） | `robots/ur/lerobot_robot_ur_v2/controllers/direct_torque.py:27-69` | $F_{world}$ → $J$ 6×6(L50) → $\tau=J^{\top}F$(L48-61) → `rtde_c.directTorque`(L61)，绕过 UR 2kHz 导纳层 |
| MuJoCo 仿真镜像（同边界） | `robots/mujoco/mujoco_robot.py`；`robots/mujoco/configuration_mujoco.py:34-39` | 任务空间命令→有界笛卡尔阻抗→`J.T@wrench`；`wrench_limits`/`torque_limits` 配置 |

---

## 系统级：分布式异步管线（图 1 三层）

| 论文描述 | 代码位置 | 说明 |
|---|---|---|
| Actor 进程执行策略、产生经验、流式传输 | `scripts/actor_server.py:62-184, 186-450` | `run_actor`（worker 线程 L118-156）、`act_with_policy`（预测→step→组装 Transition→入队） |
| Learner 进程采样双缓冲、RLPD 更新 | `scripts/learner_server.py:63, 163-345, 347` | `run_learner`、`add_actor_information_and_train`、`optimize_policy_once` |
| gRPC 传输（transitions / interactions / parameters） | `scripts/actor_server.py:480-594` | `learner_service_client`(L480)、`receive_policy`(L491)、`send_transitions`(L519) |
| 每原语独立策略注册表 + 预算 | `rl/runtime.py:88-125` | `build_adaptive_registry`：自适应原语集合、SAC 配置、`online_steps` 预算 |

---

## 倒查速查表（从代码出发）

| 想理解 | 先读 |
|---|---|
| 策略到底学什么 | `task_frame.py:94-147` |
| 一次 step 里策略/遥操作怎么合流 | `processor/action.py:393-443` |
| 干预数据怎么变成训练数据 | `env_manipulation_primitive_net.py:216-219` → `learner_server.py:590-592` |
| 双缓冲怎么采 | `learner_server.py:255-272, 387-390` |
| 力限制怎么"可证明" | `config_ur.py:168-189`（θ* 与稳定性）→ `controller.py:1442-1540`（执行） |
| 论文数字（7N→2N）在哪复现 | `config_ur.py:72-80` + `transformation_utils.py:220-227` |
