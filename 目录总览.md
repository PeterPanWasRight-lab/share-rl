# share-rl 目录总览与文件功能说明

> 基于源码精读整理（2026-08-21）。覆盖 `src/share`（包主体）、`src/experiments`、`examples`、`tests`、`tools`、`wiki`、`calibration` 及根目录文件。标注 ⚠️ 的为**未提交**（git 工作区新增）或**不可用**（引用缺失）的文件。

---

## 0. 顶层结构

```
share-rl/
├── pyproject.toml / setup.py / requirements.txt / LICENSE     # 安装与许可证（Apache-2.0, v0.0.4）
├── README.md                                                  # 使用文档
├── AGENTS.md                                                  # 给 AI 代理的架构守则
├── src/
│   ├── share/            # 主包（本大纲主体）
│   └── experiments/      # 实验 env（FoundationPose 拾取、fiddle-out）
├── examples/             # 13 个可运行示例
├── tests/                # 34 个测试
├── tools/                # 手眼标定 / SAM3 客户端
├── wiki/                 # Sphinx 文档源
└── calibration/          # 手眼标定结果 JSON
```

---

## 1. `src/share/envs/` — 核心抽象（MP / MP-Net / Transition）

### `manipulation_primitive/`
| 文件 | 功能 |
|---|---|
| `task_frame.py` | **TaskFrame 轴级控制契约**：`ControlSpace`/`ControlMode`/`PolicyMode` 枚举、6D 目标/原点/边界、可学轴推断（`learnable_axis_indices`/`is_adaptive`）、策略动作维度推断（绝对旋转轴 S¹/S²/SO(3) 流形编码）、动作 key 与特征 schema 生成、JSON 序列化 |
| `config_manipulation_primitive.py` | **原语配置层**：`ManipulationPrimitiveConfig`（draccus 注册）、处理器配置（`ObservationConfig`/`GripperConfig`/`KinematicsConfig`/`EventConfig` 等）、`make()` 构建 env+双流水线、`on_entry`/`resolve_targets` 入口钩子、`infer_features` 推断策略特征；注册子类 `zero_ft`/`move_delta`/`open_loop_trajectory`；`PrimitiveEntryContext` 原语切换上下文 |
| `env_manipulation_primitive.py` | **原语运行时**：`ManipulationPrimitive(gym.Env)`（step/reset/stop/apply_task_frames、运行时目标/完成标志/轨迹进度、跨原语共享 runtime values）；`OpenLoopTrajectoryPrimitive`（脚本化开环轨迹按内/外环频率切子步执行） |
| `__init__.py` | 延迟导出（避免循环导入） |

### `manipulation_primitive_net/`
| 文件 | 功能 |
|---|---|
| `config_manipulation_primitive_net.py` | `ManipulationPrimitiveNetConfig`：图配置（start/reset/primitives/transitions/fps/robot/teleop/cameras），`__post_init__` 做图不变量校验（非终态必须有出边、终态从 start 可达） |
| `env_manipulation_primitive_net.py` | **MP-Net 编排器**：连接硬件、为每个原语 make env+processors、五段式 step（动作处理→env 执行→观测处理→info 构建→转移检查）、整集重置路径（reset→沿图走到 start）、原语切换入口钩子 |
| `transitions.py` | **声明式图边**：`Transition` 基类 + `Outcome`；11 个子类（always/on_event/on_success/on_failure/on_observation_threshold/on_time_limit/reward_classifier/on_info_equals/all_of/record_world_pose/on_target_pose_reached） |
| `__init__.py` | 包导出 |

### 其他
| 文件 | 功能 |
|---|---|
| `utils.py` | 共享工具：`check_task_frame_robot`（机器人是否支持任务帧）、`check_delta_teleoperator`（是否 delta 遥操作）、每机器人配置归一化（`copy_per_robot`）、入口位姿解析（`resolve_entry_start_pose`）、任务帧原点观测解析、`env_to_dataset_features`、标量比较工具 |
| `ENVS.md` | env 设计文档 |

---

## 2. `src/share/processor/` — 数据流三步流水线

| 文件 | 功能 |
|---|---|
| `action.py` | **动作侧 processor**：`ToNestedActionProcessorStep`（扁平策略 tensor 拆成按 key 字典）、`MatchTeleopToPolicyActionProcessorStep`（遥操作归一化到策略学习空间：delta 直通/绝对关节 FK 差分）、`InterventionActionProcessorStep`（干预覆盖 + 学习空间动作投影回完整任务帧命令 + 绝对旋转流形解码）、`DiscretizeGripperProcessorStep`、`ToJointActionProcessorStep`（非任务帧机器人 IK 转关节）、`RelativeFrameActionProcessor` |
| `observation.py` | **观测侧 processor**：`StateObservationProcessor`（拼 `observation.state`、数值微分速度、帧堆叠）、`ImageObservationProcessor`（裁剪/缩放/归一化）、`JointsToEEObservation`（FK 注入 EE 位姿）、`RelativeFrameObservationProcessor`（相对参考位姿）、`JointVelocityProcessorStep`/`MotorCurrentProcessorStep` |
| `info.py` | **info 注入 processor**：`AddTeleopActionAsComplimentaryDataStep`（干预时读遥操作）、`AddTeleopEventsAsInfoStep`、`AddFootswitchEventsAsInfoStep`（脚控线程）、`AddKeyboardEventsAsInfoStep`（键盘事件线程） |
| `utils.py` | `flatten_nested_policy_action`（学习空间动作压平）、`policy_action_keys_for_robot`、`FootSwitchHandler`（evdev 脚控线程） |

---

## 3. `src/share/workspace/` — MP-Net 配置工作区（实验性）

| 文件 | 功能 |
|---|---|
| `mpnet.py` | **配置纯数据层**：模板创建/JSON 加载保存（draccus + 无依赖 fallback）/校验/摘要（`summarize_mpnet`、`summarize_mpnet_debug`）；结构化编辑（add/remove primitive、set_learnable_axes、set_axis_targets、add/remove transition、attach_policy…），统一由 `apply_edit(operation, args)` 分发 |
| `runtime.py` | `AgentRuntime`：受控 LLM 工具代理骨架（多轮工具调用 + 关键操作需人类确认）⚠️ **引用不存在的 `providers.py`/`store.py`/`tools.py`，不可运行** |
| `__init__.py` | 延迟导出蓝图：声明了 store/providers/tools/runtime 应提供的全部符号 |

---

## 4. `src/share/rl/` — 分布式 RL 运行时

| 文件 | 功能 |
|---|---|
| `runtime.py` | **RL 核心助手**：`build_adaptive_registry`（收集带 SAC 策略的自适应原语 + online_steps 预算）、`make_policies_for_registry`/`make_policy_processors`（actor/learner 共用策略构建与归一化）、`resolve_policy_dataset_stats`/`flatten_stats_for_config`（数据集统计解析）、`preprocess_replay_batch`、`apply_parameter_updates_from_queue`（从 gRPC 队列同步权重）、`PrimitiveBudgetCounter`（按原语步数预算） |

---

## 5. `src/share/policies/` — 策略层

| 文件 | 功能 |
|---|---|
| `__init__.py` | 导出 SAC-DAgger 与奖励分类器 |
| `sac_dagger/configuration_sac_dagger.py` | `SACDaggerBCConfig`（SAC + `bc_lr`） |
| `sac_dagger/modeling_sac_dagger.py` | `SACDaggerBCPolicy`：SAC 上叠加 BC 更新（`compute_loss_bc`：负对数似然 + MSE），干预段离线数据行为克隆 |
| `reward_classifier/configuration_state_reward_classifier.py` | `StateRewardClassifierConfig`（MEAN_STD 归一化、state_hidden_dim） |
| `reward_classifier/modeling_state_reward_classifier.py` | `StateRewardClassifier`：图像分类器 + 状态分支 MLP 的奖励分类器（供 `reward_classifier` 转移与成功检测） |
| 两个 `__init__.py` | 包导出 |

---

## 6. `src/share/scripts/` — 应用入口

| 文件 | 功能 |
|---|---|
| `record.py` | **核心录制循环**（`share-record`）：策略/遥操作切换、按原语一个数据集、干预段可选单独保存、OU 探索噪声、Rerun 可视化 |
| `design_constraints.py` | 交互式任务帧标定（`share-design-constraints`）：热键 o/r/p/s/q 设原点、重置/打印/保存边界（⚠️ 保存调用被注释，实际不生效） |
| `actor_server.py` | 分布式训练 **Actor 端**：连接 learner、构建每原语策略、执行 rollout、流式上传转移/交互指标、同步 learner 权重（gRPC） |
| `learner_server.py` | 分布式训练 **Learner 端**：双缓冲（online+offline）等比例采样、SAC+BC 更新、gRPC LearnerService、checkpoint 保存 |
| `train_reward_classifier.py` | 在 rollout 数据集上训练奖励分类器（成功尾部帧标正样本） |
| `plotting/plot_reward_classifier.py` | 同时训练 vision / vision+state 变体并输出 PDF 报告 |
| `eval_on_dataset.py` | 离线单集多策略对比绘图（⚠️ 硬编码策略路径 + 依赖外部 `beast_refiner`） |
| `measure_wrench.py` | 实时末端 wrench 六轴监控图 |
| `visualize.py` | 薄封装 LeRobot 数据集可视化 |
| `mujoco_demo.py` ⚠️ | MuJoCo 插入演示：键盘辅助插入 episode（未提交） |
| `utils/_mpl_camera_tool.py` | matplotlib 相机取景工具 |
| `utils/enumerate_evdev_devices.py` | 枚举 evdev 输入设备（找脚控开关） |
| `utils/enumerate_hid_devices.py` | 枚举 HID 设备 |
| `utils/find_crop_params.py` | 交互式拖框确定相机裁剪参数 |

> `train.py`（share-train 目标）与 `robot_workspace.py`（share-workspace 目标）**文件不存在**（git 历史从未提交）。

---

## 7. `src/share/robots/` — 机器人集成

### `ur/lerobot_robot_ur/`（v1，独立可安装包）
| 文件 | 功能 |
|---|---|
| `ur.py` | `UR(Robot)`：UR5e 封装；`set_task_frame`/`send_action`（锁定控制空间）、连接流程（controller→zero_ft→WrenchMonitor→Robotiq→相机） |
| `config_ur.py` | `URConfig`：RTDE 参数、频率 500Hz、力模式参数、安全限制、**自适应限力曲线**（`compliance_adaptive_limit_theta` 解析 + 振荡检测） |
| `controller.py` | `RTDETaskFrameController`（独立进程 1kHz RTDE 环 + 共享内存 IPC）：任务帧命令队列、虚拟目标积分、wrench/torque 输出、forceMode/moveL/servoL/关节力矩路径 |
| `mock_ur.py` / `config_mock_ur.py` | 无硬件 MockUR |
| `wrench_monitor.py` | 独立进程六轴力可视化（measured/filtered/nominal/commanded/adaptive_limit） |
| `__init__.py` / `pyproject.toml` / `README.md` | 包导出与独立安装配置 |

### `ur/lerobot_robot_ur_v2/`（控制器可插拔）
| 文件 | 功能 |
|---|---|
| `ur_v2.py` | `URV2(UR)`：按 config 选择控制器后端 |
| `config_ur_v2.py` | `URV2Config`：新增 `controller` 字段（`--env.robot.controller.type=direct_torque`） |
| `controllers/config.py` | `ControllerConfig` 判别联合基类 |
| `controllers/base.py` | `RTDETaskFrameControllerV2`：v1 重写（不依赖 v1 包），子类须实现 `_send_task_wrench`/`_enter_task_force_mode`/`_cleanup_rtde` |
| `controllers/direct_torque.py` | `DirectTorqueController`：$\tau=J(q)^\top F_{world}$ 直接力矩（绕过 UR 导纳层，接触任务） |
| `controllers/force_mode.py` | `ForceModeController`：复刻 v1 forceMode 路径 |

### `viperx/`（纯关节机器人）
| 文件 | 功能 |
|---|---|
| `viperx.py` | `ViperX(Robot)`：Dynamixel 串口 9 电机（含 shadow 同步）、纯关节 `Goal_Position` 控制 |
| `config_viperx.py` / `mock_viperx.py` / `config_mock_viperx.py` | 配置与 mock |
| `pyproject.toml` | 独立安装包 |

### `mujoco/` ⚠️（未提交，用户新实验）
| 文件 | 功能 |
|---|---|
| `configuration_mujoco.py` | `MujocoRobotConfig`：UR5e MuJoCo 后端（阻抗 kp/kd、wrench/torque 限制、夹具随机化） |
| `mujoco_robot.py` | `MujocoRobot`：实现 LeRobot Robot 契约，`send_action` 推进仿真，任务空间命令→有界笛卡尔阻抗→`J.T@wrench` 关节力矩 |
| `registry.py` | 机器人注册表（相机按 robot_id 取机器人） |
| `assets/README.md` | 场景资产说明 |

---

## 8. `src/share/teleoperators/` — 遥操作

| 文件 | 功能 |
|---|---|
| `utils.py` | `TeleopEvents` 枚举（SUCCESS/FAILURE/IS_INTERVENTION/STOP_RECORDING…）+ `has_event`/`is_intervention` |
| `delta_keyboard/`（独立包） | `KeyboardVelocityTeleop`：`{x,y,z,rx,ry,rz}.vel` 速度命名空间 + 任意事件键绑定 |
| `spacemouse/`（独立包） | `SpaceMouse`：HID 驱动（独立进程读线程）、`{x,y,z,rx,ry,rz}.vel` + gripper、按钮映射事件 |
| `widowx/`（独立包） | `WidowX`：绝对关节遥操作（Dynamixel），与 ViperX 同构 |
| `gello/` | `Gello`：绝对关节遥操作；⚠️ `name="widowx"`，calibrate/send_feedback 为 stub |
| 各 `pyproject.toml` | 独立安装包 |

---

## 9. `src/share/cameras/`、`grippers/`、`motors/`、`primitives/`

| 文件 | 功能 |
|---|---|
| `cameras/camera_realsense_depth.py` | `RealSenseDepthCamera`：RGB+深度异步读、内参/深度转米 |
| `cameras/configuration_realsense_depth.py` | `RealSenseDepthCameraConfig`（`intelrealsense_depth`） |
| `cameras/mock_camera.py` | 全黑帧 Mock 相机 |
| `cameras/mujoco_camera.py` ⚠️ | 从 MuJoCo 场景命名相机渲染图像 |
| `grippers/robotiq_controller.py` | 独立进程驱动 Robotiq 2F-85（TCP 63352 字符串命令） |
| `motors/dynamixel/dynamixel.py` | `TrossenDynamixelBus`：Dynamixel 总线扩展（校准/归一化） |
| `primitives/foundationpose.py` | `FoundationPosePrimitive`：深度相机+FoundationPose 估位姿→手眼标定→写 runtime value `object_pose`→按容差写 SUCCESS/FAILURE；另注册 3 个辅助原语 ⚠️ 与 experiments 副本重复注册同名子类 |

---

## 10. `src/share/configs/`、`utils/`、`debug/`

### configs/
| 文件 | 功能 |
|---|---|
| `record.py` | `RecordConfig`（录制参数：use_policy/save_only_interventions/OU 探索噪声/stop_after_terminal…） |
| `rl.py` | `MPNetTrainRLServerPipelineConfig`（分布式 SAC 训练配置、role 专属输出目录） |
| `mpnet.py` | `DatasetRecordConfig`（数据集录制参数）、`TrainRLServerPipelineConfig` |
| `design_constraints.py` / `measure_wrench.py` | 标定/力监控工具配置 |
| `mujoco_insertion.py` ⚠️ | MuJoCo 插入任务 MP-Net 配置（脚本化 + 可学插入轴） |

### utils/
| 文件 | 功能 |
|---|---|
| `transformation_utils.py` | 旋转流形工具（extrinsic XYZ、S¹/S²/SO(3) 编解码）、任务帧↔世界坐标变换、delta 位姿合成、CCW 弧区间、**`exp_scale`（论文自适应力限制 Eq(4) 的实现）**、观测位姿读取 |
| `kinematics.py` | `RobotKinematics`（URDF）+ `MRKinematics`（modern_robotics 空间旋量，vx300s/wx250s 预置） |
| `control_utils.py` | `make_policies_and_datasets`（按原语建数据集+策略+processor）、`predict_action`（推理统一入口）、`make_step_timing_hooks`、`MPNetStepCounter` |
| `shared_memory.py` | `SharedMemoryQueue`/`RingBuffer`/`SharedNDArray` 零拷贝 IPC 原语 |
| `video_utils.py` | `MultiVideoEncodingManager`（中断时批量编码清理） |
| `env_config_snapshot.py` | 数据集随附 env 配置快照（保存/加载/对比） |
| `logging_utils.py` | `log_runtime_frequency`/`primary_loss` |
| `device.py` / `mock_utils.py` / `constants.py` | 设备选择 / MockRobot 测试辅助 / `DEFAULT_ROBOT_NAME` |

### debug/
| 文件 | 功能 |
|---|---|
| `mpnet_debug.py` | `MPNetDebugger`/`MPNetDebugSession`：异步 MP-Net 调试——JSONL trace + Rerun 实时可视化（图节点/转移/当前与目标位姿/误差） |

---

## 11. `src/experiments/` — 实验 env

| 文件 | 功能 |
|---|---|
| `envs/foundationpose/primitives.py` | FoundationPose 原语早期副本（⚠️ 与 share/primitives 重复注册） |
| `envs/foundationpose/ur5e_foundationpose_pick.py` | 完整 UR5e 视觉拾取管线：13 个原语编排（scan→estimate→grasp→insert→复位），读 `poses.json` |
| `envs/fiddle_out.py` | `EEFiddleOutCirclePrimitiveConfig`（EE 帧 XY 圆轨迹）+ SpaceMouse 遥操作编排 |

---

## 12. `examples/` — 13 个可运行示例

| 文件 | 功能 |
|---|---|
| `demo_mp_net.py` / `demo_mp_net_real.py` | MP-Net 基础示例（mock / 真机） |
| `demo_mp_net_mock_bookshelf.py` / `_mock_tea_pour.py` / `_mock_dual_arm_handover.py` | Mock 场景：书架拾放/倒茶/双臂交接 |
| `demo_teleop.py` / `demo_keyboard.py` / `demo_jzilke.py` / `demo_ki_akademie.py` | 遥操作演示（键盘/SpaceMouse/定制场景） |
| `demo_task_frame_controller.py` | 任务帧控制器直测 |
| `grasp_demo.py` / `manual_grasp_pose.py` | 抓取位姿演示/手动标定 |
| `_mock_mp_net_utils.py` | mock 场景共享工具 |

## 13. `tests/` — 34 个测试（按模块镜像 src）

`tests/share/envs/`（TaskFrame/原语配置/MP-Net step/transitions/干预/动作匹配/UR 集成…）、`tests/share/scripts/`（record/design_constraints/measure_wrench/**learner_replay_buffers**/**mpn_rl_servers_e2e**）、`tests/share/policies/`（sac_dagger_bc）、`tests/share/rl/`（policy stats）、`tests/share/configs/`（record/rl/measure_wrench/序列化）、`tests/share/workspace/`、`tests/share/debug/`、`tests/experiments/`、`tests/share/robots/test_mujoco_robot.py` ⚠️

## 14. `tools/` — 工程工具

| 文件 | 功能 |
|---|---|
| `hand_eye_calibration.py` | UR 手眼标定 CLI（棋盘格、49 位姿网格、auto/manual） |
| `hand_eye_lib.py` | 标定核心库（TSAI 求解、URRobot、RealSenseD405 封装） |
| `save_robot_poses.py` | 已废弃（转发 manual 模式） |
| `test_sam3_client.py` | SAM3 ZeroMQ 分割客户端测试 |
| `run_pose_publishers_tmux.sh` | tmux 启动 sam3/FoundationPose 服务 |

## 15. `wiki/` — Sphinx 文档

`index.rst`/`quickstart.rst`/`architecture.rst`/`connector_setup.rst`/`examples.rst`/`open_problems.rst`/`mujoco.rst` ⚠️/`conf.py`/`README.md`

## 16. `calibration/`

三个手眼标定结果 JSON：`hand_eye_calibration_result_ur3e.json`、`ur3e_flansch.json`、`ur5e.json`
